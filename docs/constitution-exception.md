# Proxy / Constitution Exception Process

If provider CORS or secret handling prevents direct client calls, follow the project's constitution-exception process:

1. Document the technical limitation and proposed proxy design in `docs/`.
2. Submit the exception request with rationale and security mitigation plan.
3. Obtain approval before implementing any server-side proxy that would handle secrets.
